DATABASE_URL=postgres://postgres:postgres@localhost:5432/woodcarver
JWT_SECRET=change-me-in-every-environment
PORT=4000
CORS_ORIGIN=http://localhost:5173
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
# Set to `require` when the managed Postgres instance enforces TLS.
# PGSSLMODE=require
